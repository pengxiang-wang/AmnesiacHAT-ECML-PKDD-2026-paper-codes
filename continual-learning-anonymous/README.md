# Anonymous Continual Learning Package

**An anonymized machine learning package for continual learning research**

This is an anonymized Python package for Continual Learning (CL) research. It provides an integrated environment and various APIs for conducting CL experiments, as well as implemented CL algorithms and datasets that can be used directly. In addition to continual learning, the package also supports continual unlearning, multi-task learning, and single-task learning.

External documentation links are omitted in the anonymous release: `<ANONYMIZED_URL>`.

The generated API reference is omitted in the anonymous release.
